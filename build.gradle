apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "Event"
version = '1.0'
mainClassName = "Event"

repositories {
  mavenCentral()
}

dependencies{
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
